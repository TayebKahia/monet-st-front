import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove static export settings for Vercel deployment
  // Vercel handles Next.js optimally without these
};

export default nextConfig;
